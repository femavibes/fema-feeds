# @cfb/l1-registry

Defines the **L1 filter contract** and **fixed step order**.

Individual filter implementations live in `@cfb/l1-filters`.

## Fixed step order

See `src/step-order.ts`. Do not reorder without updating docs/PLAN.md §3.3.
