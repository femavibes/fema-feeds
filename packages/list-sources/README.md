# @cfb/list-sources

Resolves author list **sources** → `Set<did>`.

Supports `manual_dids` and `bluesky_list` (`at://` and `https://bsky.app/...` URLs).

Does not depend on L1, Jetstream, or Postgres.
